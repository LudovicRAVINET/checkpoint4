{% extends 'base.html.twig' %}

{% block title %}New VehicleType{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row justify-content-around">
            <div class="row col-sm-4 justify-content-around">
                <h1 class="mx-4">Créer un nouveau type de véhicule</h1>

                {{ include('vehicle_type/_form.html.twig', {'button_label': 'Créer'}) }}

                <a class="btn btn-primary mx-4 w-25" href="{{ path('vehicle_type_index') }}">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}
