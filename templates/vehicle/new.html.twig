{% extends 'base.html.twig' %}

{% block title %}New Vehicle{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row justify-content-around">
            <div class="row col-sm-4 justify-content-around">
                <h1 class="mx-4">Créer un nouveau véhicule</h1>

                {{ include('vehicle/_form.html.twig', {'button_label': 'Créer'}) }}

                <a class="btn btn-primary mx-4 w-25" href="{{ path('vehicle_index') }}">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}
