{% extends 'base.html.twig' %}

{% block title %}Edit Booking{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row justify-content-around">
            <div class="row col-sm-4 justify-content-around">
                <h1 class="mx-4 text-center">Edition de la réservation</h1>

                {{ include('booking/_form.html.twig', {'button_label': 'Mise à jour'}) }}

                <a class="btn btn-primary w-25 mx-4" href="{{ path('booking_index') }}">Retour à la liste</a>

                {{ include('booking/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
